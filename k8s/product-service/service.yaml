apiVersion: v1
kind: Service
metadata:
  name: product-service
  namespace: ctse-app
  labels:
    app: product-service
spec:
  selector:
    app: product-service
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 3302
      targetPort: 3302
