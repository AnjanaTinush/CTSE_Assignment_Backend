apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: ctse-app
  labels:
    app: auth-service
spec:
  selector:
    app: auth-service
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 3301
      targetPort: 3301
