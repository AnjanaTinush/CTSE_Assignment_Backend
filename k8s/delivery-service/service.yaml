apiVersion: v1
kind: Service
metadata:
  name: delivery-service
  namespace: ctse-app
  labels:
    app: delivery-service
spec:
  selector:
    app: delivery-service
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 3304
      targetPort: 3304
